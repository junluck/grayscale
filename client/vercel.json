{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" },  
    { "src": "build/**/*", "use": "@vercel/static-build" }  
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/server.js" },  
    { "src": "/(.*)", "dest": "/build/$1" }  
  ]
}